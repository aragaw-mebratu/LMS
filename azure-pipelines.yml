trigger:
- master

variables:
  IMAGE_NAME: aragawmebratu/lms
  IMAGE_TAG: $(Build.BuildId)

stages:
- stage: BuildAndPublish
  displayName: Build & Push Docker Image
  jobs:
  - job: Docker
    displayName: Docker Build Job
    pool:
      name: SelfHosted   # ✅ Linux agent (recommended)

    steps:
    - checkout: self

    # 🔐 Login to Docker Hub
    - task: Docker@2
      displayName: Docker Hub Login
      inputs:
        command: login
        containerRegistry: DockerHub   # service connection name

    # 🐳 Build & Push Docker Image
    - task: Docker@2
      displayName: Build & Push Backend Image
      inputs:
        command: buildAndPush
        repository: $(IMAGE_NAME)
        dockerfile: Dockerfile.backend
        tags: |
          $(IMAGE_TAG)

    # 📝 Update docker-compose.yml (BASH — no PowerShell!)
    - task: Bash@3
      displayName: Inject image tag into docker-compose.yml
      inputs:
        targetType: inline
        script: |
          echo "Injecting IMAGE_TAG=$(IMAGE_TAG)"
          sed "s|\${IMAGE_TAG}|$(IMAGE_TAG)|g" docker-compose.yml \
            > $(Build.ArtifactStagingDirectory)/docker-compose.yml

    # 📦 Publish docker-compose.yml
    - task: PublishBuildArtifacts@1
      displayName: Publish docker-compose artifact
      inputs:
        PathtoPublish: $(Build.ArtifactStagingDirectory)/docker-compose.yml
        ArtifactName: docker-compose
        publishLocation: Container
